# Public application URL used by metadata and future callback configuration.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Environment identifier. Keep values predictable across local, preview, staging, production.
APP_ENV=development

# Logging threshold used by the platform logger foundation.
LOG_LEVEL=info
